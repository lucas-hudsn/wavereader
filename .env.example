# Copy to .env and fill in. Never commit .env.
# HF token (https://huggingface.co/settings/tokens) — router provider default.
HF_TOKEN=
# NVIDIA NIM key (build.nvidia.com) — provider switch alternative.
NVIDIA_API_KEY=
