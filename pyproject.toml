[project]
name = "wavereader"
version = "0.1.0"
description = "Surf break data generator and API"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=1.2.0",
    "ddgs>=9.16.0",
    "fastapi[standard]>=0.141.1",
    "huggingface-hub>=1.29.0",
    "langchain-community>=0.4.2",
]
